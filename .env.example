# Optional Supabase auth/storage
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional server-side AI provider. Never put provider secrets in VITE_ variables.
# Connect your own /api/ai endpoint or serverless function for real model calls.
AI_PROVIDER=
AI_API_KEY=
